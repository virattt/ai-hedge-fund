# Custom integrations — Alpaca broker and live trading
#
# This folder is owned by our fork and kept separate from upstream src/ and v2/
# so we can pull upstream changes without merge conflicts.
#
# Modules:
#   integrations/alpaca/  — broker, execution, CLI
#   integrations/data/    — Alpaca + Finnhub composite data provider
#
# Sync upstream:
#   git fetch upstream
#   git merge upstream/main
